SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
# shellcheck source=profiles/generic.conf
source "${SCRIPT_DIR}/profiles/generic.conf"

EXPECTED_CPU_COUNT=12
GPU_DMESG_PATTERN="Mali-G720-Immortalis"
MIN_ETH_INTERFACES=2
MODULE_SMOKE_TEST="scmi_hwmon panthor"
TEMP_CPU_WARN=70000
TEMP_CPU_FAIL=85000

GPU_EXPECTED_DRIVER="panthor"
GPU_EXPECTED_MODULES="panthor"

AUDIO_MIN_CARDS=1
AUDIO_EXPECTED_CARD="HDMI"
AUDIO_DMESG_PATTERN=""
